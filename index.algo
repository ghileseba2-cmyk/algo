ALGORITHM twosets
VAR
    set1:[3, 1, 7, 9];
    set2:[2, 4, 1, 9, 3];
    i,j:INTEGER;
    sum1,sum2:INTEGER;
BEGIN
    sum:=0
    IF (set1[i]=set2[j]) THEN
        return(correct);
    else
     sum=sum+set[i]  
    END_FOR
    IF (set2[j]=set1[i]) THEN
        return(correct)
        else
        sum2=sum1+set2[j]
    END_IF

    return(sum2)
END